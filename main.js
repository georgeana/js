let name = "georgeana";

let numKids = 1;

let address = "Brunevang 24";

let city = "Copenhagen";

let namePartner = "Stefan";

let ocupation = "freelancer";

let info = name + "will be a" + ocupation + "in" + city + "and married to" + namePartner + "with" numKids + "kid";
console.log(info);

